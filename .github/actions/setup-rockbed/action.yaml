name: Rockbed
description: Setup Rockbed
inputs:
  version:
    required: false
    default: main
    description: The version of Rockbed to setup
runs:
  using: "composite"
  steps:
    - name: Run Devnet
      shell: bash
      run: |
        pwd
        ls -alh